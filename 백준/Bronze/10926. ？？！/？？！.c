#define _CRT_SECURE_NO_WARNINGS
#define SIZE(a) (sizeof(a)/sizeof((a)[0]))

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(void) {
	char str[50];
	scanf("%s", str);
	printf("%s??!", str);

	return 0;
}
